import { useEffect,useState } from 'react';import { getPublicMembers } from '../lib/dataService';import type { Member } from '../lib/types';
export function Members(){const[members,setMembers]=useState<Member[]>([]);useEffect(()=>{void getPublicMembers().then(setMembers)},[]);return <section><h1>部員紹介</h1>{members.length===0?<p className="card">部員情報は現在準備中です。</p>:<div className="member-grid">{members.map(m=><article key={m.id} className={`card member ${['部長','副部長'].includes(m.role)?'leader':''}`}><div className="avatar" aria-label={`${m.vrcName}のアイコン`}>{m.iconUrl?<img src={m.iconUrl} alt=""/>:'☕'}</div><h2>{m.vrcName}</h2><p><b>{m.role}</b> / {m.assignment}</p><p>{m.bio}</p><p>好きな飲み物：{m.favoriteDrink}</p><span>{m.status}</span></article>)}</div>}</section>}
